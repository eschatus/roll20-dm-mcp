# QLoRA training deps for training/train_qlora.py — pin these; version drift in
# unsloth/trl/bitsandbytes is a frequent source of silent regressions (chat-template
# handling, GGUF export, chunked-CE support). Known-good combination as of the
# 2026-07-28 hardware-verdict measurements (SFT_RUN_STATE.md).
#
# Install order matters for unsloth: let its own installer resolve the torch/cuda
# pairing rather than pip-installing torch separately first when possible. On a
# RunPod PyTorch template, torch is usually already present — see RUNBOOK.md.

torch==2.11.0+cu130
unsloth==2026.7.5
trl==0.24.0
peft==0.20.0
bitsandbytes==0.50.0
accelerate>=1.0.0
datasets>=2.20.0
transformers>=4.46.0
sentencepiece
protobuf
xformers
ninja
packaging
